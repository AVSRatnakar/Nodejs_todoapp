<html>

<head>
    <title> ToDo App </title>
    <link href="https://fonts.googleapis.com/css?family=Lato:100" rel="stylesheet">
    <link href="/styles.css" rel="stylesheet">
</head>

<body>
    <div class="container">
        <h1 class="title" style="color: black;">ToDo List App </h1>

        <form action="/addtask" method="POST">
            <input class="input" type="text" name="newtask" placeholder="add new task">
            <button class="btn"> Add Task </button>


            <h2 class="add-task" style="color: red;"> Added Task </h2>

            <% for( var i=0; i < task.length; i++){ %>
                <li><input type="checkbox" name="check" value="<%= task[i] %>" />
                    <%= task[i] %>
                </li>
                <% } %>

                    <button class="btn1" formaction="/removetask" type="submit" id="top"> Remove </button>
        </form>

        <h2 style="color: red;"> Completed task </h2>

        <% for(var i=0; i < complete.length; i++){ %>
            <li><input type="checkbox" checked>
                <%= complete[i] %>
            </li>
            <% } %>

    </div>

</body>

</html>